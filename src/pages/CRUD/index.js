import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const CRUD = () => {
    return (
        <View>
            <Text>tes</Text>
        </View>
    )
}

export default CRUD

const styles = StyleSheet.create({})
